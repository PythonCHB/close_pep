#!/usr/bin/env python3

"""
some very simple code to see if is_close C module even semi works
"""

import is_close_module

print(is_close_module.is_close(1, 1))

print(is_close_module.is_close(10, 11))

print (is_close_module.is_close(1.1111111111, 1.1111111111))
